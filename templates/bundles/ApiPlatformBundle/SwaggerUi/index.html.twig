<!DOCTYPE html>
<html>
<head>
    {% block meta %}
        <meta charset="UTF-8">
    {% endblock meta %}
    <title>{% block title %}{{ swagger_data.spec.info.title }}{% endblock title %}</title>
    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('bundles/apiplatform/fonts/open-sans/400.css') }}">
        <link rel="stylesheet" href="{{ asset('bundles/apiplatform/fonts/open-sans/700.css') }}">
        <link rel="stylesheet" href="{{ asset('bundles/apiplatform/swagger-ui/swagger-ui.css') }}">
        <link rel="stylesheet" href="{{ asset('bundles/apiplatform/style.css') }}">
    {% endblock stylesheets %}

    {% block swagger_data %}
        {# json_encode(65) is for JSON_UNESCAPED_SLASHES|JSON_HEX_TAG to avoid JS XSS #}
        <script id="swagger-data" type="application/json">{{ swagger_data|json_encode(65)|raw }}</script>
    {% endblock swagger_data %}

    <style>
        body {
            margin: 0 auto !important;
        }
    </style>
    <script>
        setTimeout(() => {
            let dl = Array.from(document.getElementsByTagName('p'))
             .find(el => el.textContent.includes('Download OpenAPI specification'));
            let pw = Array.from(document.getElementsByTagName('a'))
             .find(el => el.textContent.includes('Documentation Powered by ReDoc'))
            dl.style.visibility = "hidden"
            pw.style.visibility = "hidden"
        }, 200);

    </script>
</head>
<body>
{#<header>#}

{#</header>#}

{% block swagger_ui %}
    <div id="swagger-ui" class="api-platform"></div>
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('bundles/apiplatform/redoc/redoc.standalone.js') }}"></script>
    <script src="{{ asset('bundles/apiplatform/init-redoc-ui.js') }}"></script>
{% endblock javascripts %}

{#<script src="{{ asset('bundles/nelmioapidoc/init-swagger-ui.js') }}"></script>#}
{#{% block swagger_initialization %}#}
{#    <script type="text/javascript">#}
{#        window.onload = loadSwaggerUI();#}
{#    </script>#}
{#{% endblock swagger_initialization %}#}

<script>

</script>
</body>
</html>